# 🌐 Acceso al Frontend Vue.js SPA

Este documento explica cómo acceder a la aplicación Single Page Application (SPA) de Vue.js, la cual está integrada en un único archivo `index.html` y se comunica con la API de Dev.

## I. Requisitos Previos

Asegúrate de que los contenedores de Docker (Nginx, App y DB) estén en ejecución, lo cual se logra con el comando:

```bash
sudo docker compose up -d